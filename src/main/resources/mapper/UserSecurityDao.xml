<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.albumenj.dao.UserSecurityDao">
    <select id="selectByNo" resultType="UserSecurity">
        select id, user_no, phone, mail, password, permission from user_security
        where user_no = #{userNo}
    </select>

    <select id="selectById" resultType="UserSecurity">
        select id, user_no, phone, mail, password, permission from user_security
        where id = #{id}
    </select>

    <insert id="addUser">
        insert into user_security(id, user_no, phone, mail, password, permission)
        values (#{id},#{userNo},#{phone},#{mail},#{password},#{permission})
    </insert>

    <update id="update">
        update user_security
        <set>
            <if test="userNo != null">
                user_no = #{userNo},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="mail != null">
                mail = #{mail},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="permission != null">
                permission = #{permission},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="selectLimited" resultType="UserSecurity">
        select * from user_security
        where permission > #{permission} or id = #{id}
        <if test="start != null and count != null">
            limit #{start},#{count}
        </if>
    </select>

    <select id="countUser" resultType="Integer">
        select count(*) from user_security
        where permission > #{permission} or id = #{id}
    </select>

    <delete id="delete">
        delete from user_security
        where id = #{id}
    </delete>
</mapper>