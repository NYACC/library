<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.albumen.library.dao.LogDao">
    <insert id="insert">
        insert into log(id, user, ip, content, createTime)
        VALUES(#{id},#{user},#{ip},#{content},#{createTime})
    </insert>
</mapper>